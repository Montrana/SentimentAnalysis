#pragma once
/*/#include <string>
#include "Sentiment.h"

class WordData {
private:
	string word;
	string punctuation; //what the punctuation after the word was when it was read in, if there wasn't any, this string will be blank.
	double value;
	int wordOrder; //the order that the word came in in the review
	bool isCap; //if the first letter of the word was capitalized or not when read in.

public:

	WordData();
	WordData(string inWord, string inPunctuation, double inValue, int inWordOrder, bool inIsCap);

	string getWord();
	string getPunctuation();
	double getValue();
	int getWordOrder();
	bool getCapitalization();

	void setWord(string inWord);
	void setValue(double inValue);

	void changeWord(string inWord, double inValue);

	void print();

	static int totalWords;
};*/